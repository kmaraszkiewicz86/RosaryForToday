<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RosaryForToday.Presentation.Views.RosaryListView"
             Title="Różanniec na dzisiaj">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Odśwież" Order="Primary" Priority="0"
                     Command="{Binding RefreshCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid RowDefinitions="Auto, *">
            <Label Grid.Row="0" Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   FontAttributes="Bold"
                   Margin="12,8,12,0">
                <Label.Triggers>
                    <DataTrigger TargetType="Label" Binding="{Binding ErrorMessage}" Value="{x:Null}">
                        <Setter Property="IsVisible" Value="False" />
                    </DataTrigger>
                    <DataTrigger TargetType="Label" Binding="{Binding ErrorMessage}" Value="">
                        <Setter Property="IsVisible" Value="False" />
                    </DataTrigger>
                </Label.Triggers>
            </Label>

            <!-- Outer CollectionView fills the remaining space and provides scrolling -->
            <CollectionView Grid.Row="1" ItemsSource="{Binding Items}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" Padding="10" Stroke="#555" Background="#444">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="6"/>
                            </Border.StrokeShape>

                            <VerticalStackLayout Spacing="6">
                                <Label Text="{Binding RosaryTypeName}" FontAttributes="Bold" FontSize="18" />
                                <Label Text="{Binding DayOfWeek}" FontSize="14" TextColor="Gray" />

                                <Label Text="Tajemnice:" FontAttributes="Bold" Margin="0,6,0,0" />
                                <CollectionView ItemsSource="{Binding RosaryReflections}" ItemsLayout="VerticalList">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>  d
                                            <Grid Padding="4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <VerticalStackLayout Grid.Column="1">
                                                    <Label Text="{Binding Title}" FontSize="25" />
                                                    <Label Text="{Binding Content}" FontAttributes="Italic" FontSize="23" TextColor="Gray" />
                                                </VerticalStackLayout>
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>

</ContentPage>